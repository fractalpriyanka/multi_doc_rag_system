paths:
  raw_data: /tmp/data
  vector_db: /tmp/vector_db

limits:
  max_file_size_mb: 10

options:
  preview_chars: 1000

model:
  embedding_model: all-MiniLM-L6-v2

retrieval:
  top_k: 5

groq:
  model: llama-3.1-8b-instant
